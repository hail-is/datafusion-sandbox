//! Module tests for the library root's child modules; the root itself has no parent module.

mod combiner_run;
mod dataset;
mod file_order;
mod format;
mod formulation;
mod locus;
mod pipeline;
mod plan_shape;
mod process;
mod run_metrics;
mod sink;
mod sorted_table;
mod split_points;
mod support;
mod write;
